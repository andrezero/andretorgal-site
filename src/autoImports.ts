import { Code } from 'astro/components';

import { Abstract, Audio, Iframe, Image, Note, Todo, Year } from './components/mdx';

export const autoimports = {
    Abstract,
    Audio,
    Code,
    Iframe,
    Image,
    Note,
    Todo,
    Year,
};
