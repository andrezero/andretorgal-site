key: components.action.back-to
resources:
  en:
    label: Go back to “{{ target }}”
  pt:
    label: Voltar a “{{ target }}”
  cat:
    label: Torna a “{{ target }}”
  es:
    label: Volver a “{{ target }}”
