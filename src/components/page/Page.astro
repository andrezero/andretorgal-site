<div class="page">
    <slot name="header" />
    <div class="content">
        <slot />
    </div>
</div>

<style>
    .page {
        display: flex;
        flex-direction: column;
        gap: var(--layout-unit);
        padding-bottom: var(--layout-space);
    }

    .content {
        display: flex;
        flex-direction: column;
        gap: var(--layout-gap);
    }
</style>
